#include <iostream>

// this method is called when starting the program
int main(int argc, char** argv)
{
	/* write "Hello World!" to the console 
	   and jump to next line. */
	std::cout << "Hello World!" << std::endl;
	// when successful, return 0
	return 0;
}
